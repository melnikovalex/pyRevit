name: pyrevit

args:
  --verbose:
  --debug:
  --log: <log_file>

commands:
  --help:
    short: -h
    func: PrintHelp

  --version:
    short: -V
    func: PrintVersion

  env:
    func: PrintEnv
    args:
      - name: --json
    commands:

  releases:
    func: PrintReleases
    args:
    commands:

